const config = require('config');
const { orgasmic } = require('orgasmic-core');
const { initialize, run } = orgasmic;
const { router } = require('./router');

const { app,  logger } = initialize(config, router);

run();
